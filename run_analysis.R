## run_analysis.R
# 1. Merge training and test set to create one data set
# 2. Extract only the measurements on the mean and standard deviation for each meansurement
# 3. Use descriptive activity names to name the activities in the data set
# 4. Appropriately label the data set with descriptive variable names
# 5. From the data set in step 4, create a second, independent tiday data set with the avg of
# each vairable for each activity and each subject
